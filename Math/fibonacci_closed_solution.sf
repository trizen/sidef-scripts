#!/usr/bin/ruby

func fibonacci (n) {

    define S = (1.25.sqrt + 0.5)
    define T = (-S + 1)
    define U = (-T + S)

    (S**n - T**n) / U -> round(0)
}

for i in (0..20) {
    say fibonacci(i)
}
